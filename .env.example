# Copy to .env and fill values needed for current milestone.
# Never put real credentials in this example file.

# M3: script generation
OPENROUTER_API_KEY=
SCRIPT_PROVIDER=openrouter
SCRIPT_MODEL=deepseek/deepseek-v3.2

# M4: text-to-speech. In GitHub Actions, TTS_PROVIDER is a repository variable.
TTS_PROVIDER=gemini

# Gemini provider
GEMINI_API_KEY=
TTS_MODEL=gemini-2.5-flash-preview-tts
TTS_VOICE_A=Kore
TTS_VOICE_B=Algenib

# Local Kokoro provider (install requirements-kokoro.txt + espeak-ng)
KOKORO_LANG_CODE=a
KOKORO_VOICE_A=af_heart
KOKORO_VOICE_B=am_michael
KOKORO_SPEED=1.0

# Email delivery (used when ENABLE_R2_PUBLISH is not true in GitHub Actions)
SMTP_HOST=
SMTP_PORT=465
SMTP_USE_SSL=true
SMTP_USERNAME=
SMTP_PASSWORD=
EMAIL_FROM=
EMAIL_TO=

# M5: Cloudflare R2 and private RSS
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_PUBLIC_BASE_URL=
FEED_TOKEN=

# M6: failure notifications
ALERT_WEBHOOK_URL=
